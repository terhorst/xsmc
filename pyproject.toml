[build-system]
requires = ["setuptools", "wheel", "Cython>=0.29", "numpy >= 1.15", "scipy >= 1.5.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["test"]
